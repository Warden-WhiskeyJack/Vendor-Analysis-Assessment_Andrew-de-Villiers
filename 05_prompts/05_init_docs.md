Create the following docs in 06_docs/:

1) data_dictionary.md
Define each dataset we produce and all column meanings, plus allowed values.

2) change_log.md
A template to record any corrections after QA (date, file changed, before->after, why).

3) batch_log.md
A log that will be appended after each Claude batch with:
Batch ID, input file, output file, row range, vendor count, total spend,
confidence counts, duplicate flags, notes.

Write concise, reviewer-friendly content for each.
